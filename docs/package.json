{
  "name": "sparrow-docs",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "diagrams": "for f in src/assets/diagrams/*.mmd; do npx --yes @mermaid-js/mermaid-cli -i \"$f\" -o \"${f%.mmd}.svg\" -c src/assets/diagrams/mermaid-config.json --backgroundColor transparent; done",
    "dev": "npm run diagrams && astro dev",
    "build": "npm run diagrams && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.37.7",
    "astro": "^5.7.0",
    "marked": "^17.0.5",
    "sharp": "^0.33.0",
    "zod": "^3.25.76"
  }
}
