# Recipes -- Built-In Destination Catalog
Tags: recipes

The recipe catalog is a static, top-level (consumer-less) resource the UI uses
to offer preset webhook destinations. Prove it loads and is non-empty over the
live API.

## Catalog Is Served And Non-Empty
* GET recipes should return a non-empty catalog
