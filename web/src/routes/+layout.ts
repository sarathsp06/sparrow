// This layout config enables SPA (Single Page Application) mode for adapter-static.
// All pages are rendered client-side; the server produces a single index.html fallback.
export const ssr = false;
export const prerender = false;
