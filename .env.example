# Development Environment Configuration
# Copy this to .env if you want to use environment variables

# Database connection URL
# Adjust the username, password, host, and database name as needed
DATABASE_URL=postgres://localhost/riverqueue?sslmode=disable

# Alternative examples:
# DATABASE_URL=postgres://username:password@0.0.0.0:5432/riverqueue?sslmode=disable
# DATABASE_URL=postgres://user:pass@0.0.0.0/riverqueue?sslmode=require